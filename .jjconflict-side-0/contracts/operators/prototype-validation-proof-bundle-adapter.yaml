schema: operator-contract-v1
source: agents/prototype-validation-proof-bundle-adapter.md
model: gpt-high
description: Map a prototype proof bundle into prototype PR writer inputs and dispatch the writer
contract_status: minimum-generated-from-operator-sections
inputs:
- name: proof_bundle_path
  type: string
  required: true
  default_source: caller
  description: '`proof_bundle_path`: validated proof bundle from `prototype-validation-packager.md`.'
- name: pr_writer_input_bundle_path
  type: string
  required: true
  default_source: caller
  description: '`pr_writer_input_bundle_path`: adapter-owned output manifest path.'
- name: Optional
  type: string
  required: false
  default_source: caller
  description: Optional writer output path or existing PR body refresh target from the caller.
- name: writer
  type: string
  required: false
  default_source: caller
  description: Optional writer output path or existing PR body refresh target from the caller.
- name: output
  type: string
  required: false
  default_source: caller
  description: Optional writer output path or existing PR body refresh target from the caller.
- name: path
  type: string
  required: false
  default_source: caller
  description: Optional writer output path or existing PR body refresh target from the caller.
- name: existing
  type: string
  required: false
  default_source: caller
  description: Optional writer output path or existing PR body refresh target from the caller.
- name: PR
  type: string
  required: false
  default_source: caller
  description: Optional writer output path or existing PR body refresh target from the caller.
- name: body
  type: string
  required: false
  default_source: caller
  description: Optional writer output path or existing PR body refresh target from the caller.
- name: refresh
  type: string
  required: false
  default_source: caller
  description: Optional writer output path or existing PR body refresh target from the caller.
- name: target
  type: string
  required: false
  default_source: caller
  description: Optional writer output path or existing PR body refresh target from the caller.
- name: from
  type: string
  required: false
  default_source: caller
  description: Optional writer output path or existing PR body refresh target from the caller.
- name: the
  type: string
  required: false
  default_source: caller
  description: Optional writer output path or existing PR body refresh target from the caller.
- name: caller
  type: string
  required: false
  default_source: caller
  description: Optional writer output path or existing PR body refresh target from the caller.
defaults: []
outputs:
- task: default
  success_shape: Follow the operator Output Contract/Output section and final stdout sentinel.
  wrote_lines: []
errors:
- class: BLOCKED
  cause: Required input or required artifact is missing, unreadable, or outside operator scope.
  recovery: Provide the missing input or route to the correct operator.
- class: NEEDS_INPUT
  cause: A user-owned decision is required before safe completion.
  recovery: Write the question artifact when the operator convention requires one, then stop.
side_effects:
- Writes only artifacts declared by the operator body.
must_delegate: []
forbidden_direct:
- Do not bypass the operator body, anti-scope, or stop conditions.
