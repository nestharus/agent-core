schema: operator-contract-v1
source: agents/pr-justification-adjudicator.md
model: gpt-high
description: Per-round adjudicator for PR justification. Decides cull/continue per thread; when culling, assigns drop/backlog/keep verdict. Workflow stops when all threads culled.
contract_status: minimum-generated-from-operator-sections
inputs:
- name: Current
  type: string
  required: true
  default_source: caller
  description: 'Current `threads.json` with the full round history, including:'
- name: threads
  type: string
  required: true
  default_source: caller
  description: 'Current `threads.json` with the full round history, including:'
- name: Interrogator
  type: string
  required: true
  default_source: caller
  description: Interrogator demands per round
- name: demands
  type: string
  required: true
  default_source: caller
  description: Interrogator demands per round
- name: per
  type: string
  required: true
  default_source: caller
  description: Interrogator demands per round
- name: round
  type: string
  required: true
  default_source: caller
  description: Interrogator demands per round
- name: Researcher
  type: string
  required: true
  default_source: caller
  description: Researcher evidence per round
- name: evidence
  type: string
  required: true
  default_source: caller
  description: Researcher evidence per round
- name: Value
  type: string
  required: true
  default_source: caller
  description: Value assessor benefit/cost/net per round
- name: assessor
  type: string
  required: true
  default_source: caller
  description: Value assessor benefit/cost/net per round
- name: benefit
  type: string
  required: true
  default_source: caller
  description: Value assessor benefit/cost/net per round
- name: cost
  type: string
  required: true
  default_source: caller
  description: Value assessor benefit/cost/net per round
- name: net
  type: string
  required: true
  default_source: caller
  description: Value assessor benefit/cost/net per round
- name: audit_history_path
  type: string
  required: false
  default_source: caller
  description: '`audit_history_path` (optional): canonical audit-history file. If present, read the decision register, active watch signals, shared round summaries, and the `Adjudicator` role history before deciding cull, continue, or convergence.'
- name: Adjudicator
  type: string
  required: false
  default_source: caller
  description: '`audit_history_path` (optional): canonical audit-history file. If present, read the decision register, active watch signals, shared round summaries, and the `Adjudicator` role history before deciding cull, continue, or convergence.'
defaults: []
outputs:
- task: default
  success_shape: Follow the operator Output Contract/Output section and final stdout sentinel.
  wrote_lines: []
errors:
- class: BLOCKED
  cause: Required input or required artifact is missing, unreadable, or outside operator scope.
  recovery: Provide the missing input or route to the correct operator.
- class: NEEDS_INPUT
  cause: A user-owned decision is required before safe completion.
  recovery: Write the question artifact when the operator convention requires one, then stop.
side_effects:
- Writes only artifacts declared by the operator body.
must_delegate: []
forbidden_direct:
- Do not bypass the operator body, anti-scope, or stop conditions.
