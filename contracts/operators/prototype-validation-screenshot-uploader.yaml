schema: operator-contract-v1
source: agents/prototype-validation-screenshot-uploader.md
model: gpt-high
description: Upload prototype QA screenshots to GitHub user attachments and write the URL manifest
contract_status: minimum-generated-from-operator-sections
inputs:
- name: screenshot_dir
  type: string
  required: true
  default_source: caller
  description: '`screenshot_dir`: directory containing local screenshot files from the QA walkthrough.'
- name: metadata_manifest
  type: string
  required: true
  default_source: caller
  description: '`metadata_manifest`: metadata per screenshot, including `use_case_id`, `caption`, filename or relative path, and `ordering`.'
- name: use_case_id
  type: string
  required: true
  default_source: caller
  description: '`metadata_manifest`: metadata per screenshot, including `use_case_id`, `caption`, filename or relative path, and `ordering`.'
- name: caption
  type: string
  required: true
  default_source: caller
  description: '`metadata_manifest`: metadata per screenshot, including `use_case_id`, `caption`, filename or relative path, and `ordering`.'
- name: ordering
  type: string
  required: true
  default_source: caller
  description: '`metadata_manifest`: metadata per screenshot, including `use_case_id`, `caption`, filename or relative path, and `ordering`.'
- name: screenshot_url_manifest_path
  type: string
  required: true
  default_source: caller
  description: '`screenshot_url_manifest_path`: output path to write.'
- name: gh
  type: string
  required: true
  default_source: caller
  description: Repository context for `gh api` authentication and upload target.
defaults: []
outputs:
- task: default
  success_shape: Follow the operator Output Contract/Output section and final stdout sentinel.
  wrote_lines: []
errors:
- class: BLOCKED
  cause: Required input or required artifact is missing, unreadable, or outside operator scope.
  recovery: Provide the missing input or route to the correct operator.
- class: NEEDS_INPUT
  cause: A user-owned decision is required before safe completion.
  recovery: Write the question artifact when the operator convention requires one, then stop.
side_effects:
- Writes only artifacts declared by the operator body.
must_delegate: []
forbidden_direct:
- Do not bypass the operator body, anti-scope, or stop conditions.
