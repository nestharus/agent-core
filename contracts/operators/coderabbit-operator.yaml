schema: operator-contract-v1
source: agents/coderabbit-operator.md
model: gpt-medium
description: Thin wrapper for the GitHub PR-mode CodeRabbit review loop in tools/coderabbit_review_driver.py.
contract_status: minimum-generated-from-operator-sections
inputs:
- name: repo
  type: string
  required: true
  default_source: caller
  description: '`repo`: GitHub repo in `owner/name` form.'
- name: pr_num
  type: string
  required: true
  default_source: caller
  description: '`pr_num`: GitHub pull request number.'
- name: worktree_path
  type: string
  required: true
  default_source: caller
  description: '`worktree_path`: absolute path to the PR head-branch worktree.'
- name: trigger_mode
  type: string
  required: false
  default_source: caller
  description: '`trigger_mode`: optional; `incremental` by default, `full` only for'
- name: incremental
  type: string
  required: false
  default_source: caller
  description: '`trigger_mode`: optional; `incremental` by default, `full` only for'
- name: full
  type: string
  required: false
  default_source: caller
  description: '`trigger_mode`: optional; `incremental` by default, `full` only for'
- name: initial_trigger
  type: string
  required: false
  default_source: caller
  description: '`initial_trigger`: optional; `auto` by default. Use `skip` only when the'
- name: auto
  type: string
  required: false
  default_source: caller
  description: '`initial_trigger`: optional; `auto` by default. Use `skip` only when the'
- name: skip
  type: string
  required: false
  default_source: caller
  description: '`initial_trigger`: optional; `auto` by default. Use `skip` only when the'
- name: fixer_agent
  type: string
  required: false
  default_source: caller
  description: '`fixer_agent`: optional; defaults to'
defaults: []
outputs:
- task: default
  success_shape: Follow the operator Output Contract/Output section and final stdout sentinel.
  wrote_lines: []
errors:
- class: BLOCKED
  cause: Required input or required artifact is missing, unreadable, or outside operator scope.
  recovery: Provide the missing input or route to the correct operator.
- class: NEEDS_INPUT
  cause: A user-owned decision is required before safe completion.
  recovery: Write the question artifact when the operator convention requires one, then stop.
side_effects:
- Writes only artifacts declared by the operator body.
must_delegate: []
forbidden_direct:
- Do not bypass the operator body, anti-scope, or stop conditions.
