schema: operator-contract-v1
source: agents/agent-design-auditor.md
model: gpt-xhigh
description: Audit operator prompt design against operator-file format and shared design patterns. Reports LOW/MEDIUM/HIGH findings with citations. Read-only.
contract_status: minimum-generated-from-operator-sections
inputs:
- name: operator_file
  type: string
  required: true
  default_source: caller
  description: '`operator_file=<path>` required.'
- name: repo_root
  type: string
  required: true
  default_source: caller
  description: '`repo_root=<path>` required.'
- name: operator_format_ref
  type: string
  required: true
  default_source: caller
  description: '`operator_format_ref=~/ai/agents/operator-file-format.md` required unless the caller supplies an equivalent project-local reference.'
- name: design_patterns_ref
  type: string
  required: true
  default_source: caller
  description: '`design_patterns_ref=~/ai/conventions/design-patterns.md` required unless the caller supplies an equivalent project-local reference.'
- name: values_ref
  type: string
  required: false
  default_source: caller
  description: '`values_ref=<path>` optional, default `~/ai/VALUES.md`.'
- name: context_files
  type: string
  required: false
  default_source: caller
  description: '`context_files=<paths>` optional.'
- name: audit_history_path
  type: string
  required: false
  default_source: caller
  description: '`audit_history_path=<path>` optional, read-only.'
- name: report_path
  type: string
  required: false
  default_source: caller
  description: '`report_path=<path>` optional.'
- name: mode
  type: string
  required: false
  default_source: caller
  description: '`mode=<blocking|advisory>` optional, default `blocking`.'
- name: blocking
  type: string
  required: false
  default_source: caller
  description: '`mode=<blocking|advisory>` optional, default `blocking`.'
defaults: []
outputs:
- task: default
  success_shape: Follow the operator Output Contract/Output section and final stdout sentinel.
  wrote_lines: []
errors:
- class: BLOCKED
  cause: Required input or required artifact is missing, unreadable, or outside operator scope.
  recovery: Provide the missing input or route to the correct operator.
- class: NEEDS_INPUT
  cause: A user-owned decision is required before safe completion.
  recovery: Write the question artifact when the operator convention requires one, then stop.
side_effects:
- Writes only artifacts declared by the operator body.
must_delegate: []
forbidden_direct:
- Do not bypass the operator body, anti-scope, or stop conditions.
